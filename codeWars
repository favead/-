//Your task is to sort a given string. Each word in the string will contain a single number. This number is the position the word should have in the result.

//Note: Numbers can be from 1 to 9. So 1 will be the first word (not 0).

//If the input string is empty, return an empty string. The words in the input String will only contain valid consecutive numbers.




function order(words){
  let massWords = words.split(" ");
  let numbs = new Set([1,2,3,4,5,6,7,8,9]);
  let resWords = new Map();
  let resStr = '';
  for (let i = 0; i < massWords.length; i++){
    let nowStr = massWords[i];
    for (let k = 0; k < nowStr.length; k++){
      if(numbs.has(+nowStr[k])){
        resWords.set(+nowStr[k],nowStr)
        }
      }
    }
  for(let i = 1; i <= resWords.size; i++){
    if(i == resWords.size){
      resStr += resWords.get(i);
    }else{
      resStr += resWords.get(i) + ' '
    }
  }
  return resStr
}
